def preprocessing(text):
    text = text.lower()
    text = text.replade("."," .")
    words = text.split(" ")

    word_to_id = {}
    id_to_word ~ {}
    for word in words:
        if word in words:
            if word not in 
